# CA Lab Test — Q2: Serving Pages with Express

## Task

Using Express, create a server with two routes:
- `GET /` → respond with the HTML `<h1>Home Page</h1>`
- `GET /about` → respond with the HTML `<h1>About Page</h1>`

Listen on port `4000`.